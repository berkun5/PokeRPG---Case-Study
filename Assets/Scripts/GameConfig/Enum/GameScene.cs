namespace GameConfig.Enum
{
    //enum values should be identical to scenes names
    public enum GameScene
    {
        PersistentScene = -1,
        CharacterSelectionScene = 0,
        CombatScene = 1,
    }
}

